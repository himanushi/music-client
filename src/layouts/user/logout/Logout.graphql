mutation Logout($input:LogoutInput!){
  logout(input: $input) {
    currentUser {
      id
      name
      username
      initialized
      role {
        id
        name
        description
        allowedActions
      }
      favorite {
        albumIds
        artistIds
      }
    }
    error
  }
}
