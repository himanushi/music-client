mutation Login($input:LoginInput!){
  login(input: $input) {
    currentUser {
      id
      name
      username
      registered
      publicInformations {
        id
        publicType
      }
      role {
        id
        name
        description
        allowedActions
      }
      favorite {
        albumIds
        artistIds
      }
    }
    error
  }
}
