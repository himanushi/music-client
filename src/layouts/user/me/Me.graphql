query Me {
  me {
    id
    name
    username
    registered
    publicInformations {
      id
      publicType
    }
    role {
      id
      name
      description
      allowedActions
    }
    favorite {
      albumIds
      artistIds
    }
  }
}
